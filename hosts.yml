[magento]
localhost ansible_connection=local

- hosts: localhost
  vars:
    php_version: "7.4"  # Set the PHP version here

  tasks:
    - name: Your task here
      # Use the php_version variable in your task
